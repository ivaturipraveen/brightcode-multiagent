# ── REQUIRED ──────────────────────────────────────────────────────────────────
# PostgreSQL connection string (no SQLite fallback — this must be set)
DATABASE_URL=postgresql://user:password@host/dbname

# Secret key for JWT signing
JWT_SECRET=your-secret-key

# Anthropic API key for AI chat
ANTHROPIC_API_KEY=your-anthropic-api-key

# Optional: override default model
# MODEL=claude-sonnet-4-6

# ── SMTP (for WoW contact form email) ────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=fagiolicristiano@gmail.com
SMTP_PASS=oma irtf shfm tehz
# Destination for WoW Private Events / contact submissions
WOW_CONTACT_EMAIL=info@wowfinedining.com
